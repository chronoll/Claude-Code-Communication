# Agent CLI configuration (dotenv format)
#
# プロファイル切替:
#   AGENT_PROFILE=claude|cursor|gemini|codex
# ロール別上書き:
#   ROLE_PRESIDENT_BIN=... / ROLE_PRESIDENT_ARGS=...
#   ROLE_BOSS1_BIN=...     / ROLE_BOSS1_ARGS=...
#   ROLE_WORKER1_BIN=...   / ROLE_WORKER1_ARGS=...
#
# 利用エージェントを設定
AGENT_PROFILE=claude

# Claude Code
PROFILE_CLAUDE_BIN="claude"
PROFILE_CLAUDE_ARGS="--dangerously-skip-permissions"

# Cursor CLI
PROFILE_CURSOR_BIN="cursor-agent"
PROFILE_CURSOR_ARGS=""

# Gemini CLI
PROFILE_GEMINI_BIN="gemini"
# 全自動寄りにする場合は --sandbox -y を付与。手動承認に寄せるなら空にする
PROFILE_GEMINI_ARGS=""

# Codex CLI
PROFILE_CODEX_BIN="codex"
# 安全寄りデフォルト。完全無承認にする場合は --dangerously-bypass-approvals-and-sandbox へ
PROFILE_CODEX_ARGS="--ask-for-approval on-failure --sandbox workspace-write"

# 例: ロール別上書き（必要に応じてコメント解除）
# ROLE_PRESIDENT_BIN="codex"
# ROLE_PRESIDENT_ARGS="--full-auto"
# ROLE_BOSS1_BIN="cursor-agent"
# ROLE_BOSS1_ARGS=""
# ROLE_WORKER1_BIN="gemini"
# ROLE_WORKER1_ARGS="--sandbox -y"
